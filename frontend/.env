#see https://git.muenchen.de/km23/mucatar (internal only), otherwise use own hosted instance url of https://github.com/it-at-m/ad2image
VITE_AD2IMAGE_URL="https://mucatar.muenchen.de/"
# used inside the ApiFactory and defines the base path for all calls within the generated client
VITE_BASE_API_PATH="/api/backend"